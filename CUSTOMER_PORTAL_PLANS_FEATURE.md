# ویژگی طرح‌های اشتراک در پرتال مشتریان

## خلاصه
در پرتال مشتریان، مشتری می‌تواند یک طرح اشتراک را خریداری کند و با داشتن طرح فعال، ریوارد‌ها را با قیمت ارزان‌تر خریداری کند.

## تصمیمات و شفاف‌سازی‌های جدید

- ✅ هر مشتری فقط یک طرح فعال می‌تواند داشته باشد (پاسخ سؤال ۱: خیر)
- ✅ طرح‌ها پس از پایان اعتبار به صورت خودکار منقضی می‌شوند و نیازی به لغو دستی نیست (پاسخ سؤال ۲: خیر)
- ✅ ایجاد رابط مدیریت طرح‌ها در Admin Panel باید در بک‌لاگ قرار گیرد (پاسخ سؤال ۳: بلی)
- ✅ Job دوره‌ای برای منقضی کردن طرح‌های تمام‌شده پیاده‌سازی شد (پاسخ سؤال ۴: بلی)
- ✅ در فرانت‌اند قیمت اصلی و قیمت پس از تخفیف ریواردها به صورت هم‌زمان نمایش داده می‌شود (پاسخ سؤال ۵: بلی)

## امکانات پیاده‌سازی شده

### 1. لایه Domain (✅ تکمیل شده)

#### انتیتی‌ها
- **Plan**: انتیتی طرح اشتراک با ویژگی‌های زیر:
  - عنوان و توضیحات
  - قیمت به امتیاز
  - مدت اعتبار (روز)
  - نوع تخفیف (درصدی/مقداری)
  - مقدار تخفیف
  - تخفیف عمومی (به همه ریوارد‌ها) یا خاص
  - تصویر
  - وضعیت فعال/غیرفعال

- **CustomerPlan**: انتیتی طرح فعال مشتری با ویژگی‌های زیر:
  - ارجاع به مشتری و طرح
  - تاریخ خرید، شروع و انقضا
  - وضعیت (فعال/منقضی/لغو شده/معلق)
  - قیمت پرداختی
  - تعداد استفاده
  - مجموع تخفیف دریافتی
  - یادداشت

- **RewardCost**: به‌روزرسانی شده با فیلد `PlanId` برای تعیین قیمت ویژه ریوارد با طرح خاص

### 2. لایه Infrastructure (✅ تکمیل شده)

- **PlanConfiguration**: کانفیگ EF Core برای جدول Plans
- **CustomerPlanConfiguration**: کانفیگ EF Core برای جدول CustomerPlans
- **HyperContext**: به‌روزرسانی با DbSet های Plan و CustomerPlan

### 3. لایه Application (✅ تکمیل شده)

#### Queries
- **GetPlansQuery**: دریافت لیست طرح‌های فعال با پیجینگ و جستجو
- **GetPlanByIdQuery**: دریافت جزئیات یک طرح
- **GetMyActivePlanQuery**: دریافت طرح فعال مشتری

#### Commands
- **PurchasePlanCommand**: خرید طرح توسط مشتری

#### Services
- **IPlanService**: اینترفیس سرویس مدیریت طرح‌ها
- **PlanService**: پیاده‌سازی سرویس با امکانات:
  - دریافت و مدیریت طرح‌ها
  - خرید طرح و کسر امتیاز
  - محاسبه قیمت ریوارد با احتساب تخفیف طرح
  - منقضی کردن خودکار طرح‌های تمام‌شده

#### Jobs
- **IExpireCustomerPlansJob** و **ExpireCustomerPlansJob**: Job دوره‌ای (هر ۳۰ دقیقه) برای منقضی کردن طرح‌های تمام‌شده

### 4. لایه API (✅ تکمیل شده)

- **PlansController**: کنترلر REST API با endpoint های:
  - `GET /api/plans` - دریافت لیست طرح‌ها
  - `GET /api/plans/{id}` - دریافت جزئیات طرح
  - `GET /api/plans/my-active-plan` - دریافت طرح فعال من
  - `POST /api/plans/purchase` - خرید طرح

### 5. لایه Frontend (✅ تکمیل شده)

#### Services
- **plan.service.ts**: سرویس فراخوانی API های طرح

#### Pages
- **`/plans`**: صفحه لیست و خرید طرح‌های اشتراک
- **`/my-plan`**: صفحه مشاهده طرح فعال و آمار استفاده
- به‌روزرسانی صفحات `rewards/` برای نمایش همزمان قیمت اصلی و قیمت پس از تخفیف

#### Components
- **ActivePlanCard**: کارت نمایش خلاصه طرح فعال در داشبورد

## منطق محاسبه تخفیف

سرویس `PlanService.CalculateRewardPriceWithPlanDiscountAsync` منطق محاسبه تخفیف را پیاده‌سازی کرده است:

### 1. بررسی طرح فعال
- چک می‌شود که آیا مشتری طرح فعالی دارد
- اگر طرح معتبر نباشد، قیمت اصلی برگردانده می‌شود

### 2. تخفیف خاص یا عمومی
- اگر `IsGlobalDiscount = false`: قیمت خاص از RewardCost دریافت می‌شود (در صورت تعریف)
- اگر `IsGlobalDiscount = true`: تخفیف به همه ریوارد‌ها اعمال می‌شود

### 3. محاسبه تخفیف
- **تخفیف درصدی**: `قیمت نهایی = قیمت اصلی - (قیمت اصلی × درصد ÷ 100)`
- **تخفیف مقداری**: `قیمت نهایی = قیمت اصلی - مقدار تخفیف`
- حداقل قیمت: صفر

## Migration (⏳ در انتظار)

برای اجرای Migration و ایجاد جداول در دیتابیس:

```bash
cd D:\Projects\Hyper\Backend\src\Core\Hyper.Infrastructure
dotnet ef migrations add AddPlansTables --startup-project ..\..\CustomerPortal\Hyper.CustomerPortal.Api
dotnet ef database update --startup-project ..\..\CustomerPortal\Hyper.CustomerPortal.Api
```

## نکات فنی

### 1. محاسبه خودکار وضعیت
- وضعیت طرح (`CustomerPlanStatus`) به صورت خودکار توسط Job هر ۳۰ دقیقه بررسی و در صورت انقضا به‌روزرسانی می‌شود
- در صورت نیاز می‌توان بازه اجرای Job را تنظیم کرد

### 2. تراکنش خرید
- هنگام خرید طرح، امتیاز از حساب مشتری کسر می‌شود
- تراکنش در جدول `CustomerTransaction` ثبت می‌شود

### 3. آمار استفاده
- هر بار که مشتری از تخفیف طرح استفاده کند:
  - `UsageCount` افزایش می‌یابد
  - `TotalDiscountReceived` به‌روزرسانی می‌شود

## موارد آینده (اختیاری)

### 1. چند طرح همزمان
- در حال حاضر مشتری فقط می‌تواند یک طرح فعال داشته باشد
- می‌توان قابلیت داشتن چند طرح همزمان را اضافه کرد

### 2. تمدید خودکار
- امکان تمدید خودکار طرح پس از انقضا

### 3. لغو و بازگشت امتیاز
- امکان لغو طرح و بازگشت نسبی امتیاز

### 4. طرح‌های ویژه برای سگمنت خاص
- تعریف طرح‌های اختصاصی برای گروه‌های خاص مشتریان

### 5. گزارش‌گیری
- گزارش فروش طرح‌ها
- گزارش ROI طرح‌ها برای مشتری
- گزارش محبوب‌ترین طرح‌ها

### 6. Admin Panel
- رابط مدیریتی برای ایجاد و ویرایش طرح‌ها
- تنظیم قیمت‌های خاص ریوارد برای هر طرح

## تست‌های لازم

1. **تست واحد (Unit Tests)**
   - تست محاسبه تخفیف درصدی
   - تست محاسبه تخفیف مقداری
   - تست محاسبه با و بدون طرح فعال
   - تست Job منقضی‌کننده طرح‌ها

2. **تست یکپارچگی (Integration Tests)**
   - تست خرید طرح
   - تست کسر امتیاز
   - تست محاسبه قیمت ریوارد با طرح

3. **تست E2E**
   - فلوی کامل خرید طرح از UI
   - فلوی خرید ریوارد با تخفیف طرح
   - مشاهده طرح فعال و آمار

## سوالات و پاسخ‌ها

1. **آیا نیاز است مشتری بتواند چند طرح فعال همزمان داشته باشد؟** → خیر
2. **آیا باید امکان لغو طرح و بازگشت امتیاز وجود داشته باشد؟** → خیر، طرح باید براساس زمان منقضی شود
3. **آیا نیاز است برای Admin Panel هم رابط مدیریت طرح‌ها ایجاد شود؟** → بلی (در بک‌لاگ ثبت شد)
4. **آیا باید Job برای به‌روزرسانی خودکار وضعیت طرح‌ها ایجاد شود؟** → بلی (پیاده‌سازی شد)
5. **آیا باید در صفحه ریوارد‌ها، قیمت با تخفیف طرح نمایش داده شود؟** → بلی (پیاده‌سازی شد)

## نتیجه‌گیری

ویژگی طرح‌های اشتراک با موفقیت در تمام لایه‌های سیستم (Domain, Infrastructure, Application, API, Frontend) پیاده‌سازی شده است. تنها بخش باقیمانده، اجرای Migration برای ایجاد جداول در دیتابیس می‌باشد.


